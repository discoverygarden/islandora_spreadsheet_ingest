name: Code Linting
on:
  pull_request:
    branches:
      - 8.x-2.x

jobs:
  Lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2

      - name: Run CodeSniffer
        uses: nchiasson-dgi/CodeSniffer@v1.0.2
        with:
          path: ./
